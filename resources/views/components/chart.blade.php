<div id="{{ $id }}" data-chart data-chart-type="{{ $type }}" data-chart-series="{{ $series }}"
    data-chart-categories="{{ $categories }}" data-chart-series-name="{{ $seriesName }}"
    data-chart-point-format="{{ $pointFormat }}" style="width: 100%; height: {{ $height }}; margin: 0 auto"></div>
